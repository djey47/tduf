apply plugin: 'java'

sourceCompatibility = ext.sourceCompatibility
version = ext.version

repositories {
    mavenCentral()
}

dependencies {
    testCompile group: 'junit', name: 'junit', version: ext.junit.version
}